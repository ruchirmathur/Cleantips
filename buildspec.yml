version: 0.2            
phases:
  install:
    runtime-versions:
      nodejs: 10
    commands:
      - npm install npm@latest -g
      - npm install -g @angular/cli
  pre_build:
    commands:
      - echo Prebuild steps
      - npm install
  build:
    commands:
      - ng build --prod